import { Box, Typography, useTheme } from "@emotion/react";
import React from "react";
import { tokens } from "../theme";

const ProfileCard = () => {
	const theme = useTheme();
	const colors = tokens(theme.palette.mode);
	return <Box></Box>;
};
